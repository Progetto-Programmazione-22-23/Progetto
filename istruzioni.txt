# inizia a giocare: 
- per iniziare a giocare da terminale, navigare fino alla cartella contenente il progetto e digitare "make"!
- puoi navigare nel menu grazie alle frecce direzionali
    -premendo START, il gioco ha inizio. Puoi muoverti con i tasti 'wasd' e puoi saltare a sinistra (con 'q') o a destra (con 'e')!

# come uscire:
- una volta fatto partire il programma, ovunque ti trovi, se hai bisogno di chiudere il gioco ti basterà premere ESC (per salvare) o ctrl+C (utile in fase di test nel caso si bloccasse qualcosa).

#items.txt
(id) (prezzo) (nome-del-item) (!attivo/passivo) (id item da cui viene upgradato) (valore generico) (desc-del-item) (bonus health) (ad) (ap) (res) (speed)

id <= 10, spade meelee
<= 20, archi
<= 30, bacchette
<= 40, scudi
<= 50, consumabili pozioni potenziamenti temp
>50, armor power up permanenti


